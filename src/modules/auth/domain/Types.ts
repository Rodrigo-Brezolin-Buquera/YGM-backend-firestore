export interface loginDTO {
    email: string,
    password: string
}

export interface createUserDTO {
    id: string,
    email: string,
    name: string
}

export interface userIdDTO {
    id: string,
}